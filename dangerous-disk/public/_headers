# Content-hashed build assets (JS chunks, Monaco, web workers, fonts, CSS).
# Filenames change whenever content changes, so they are safe to cache forever.
/_astro/*
  Cache-Control: public, max-age=31536000, immutable
